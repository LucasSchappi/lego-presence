// Site settings. See README → "Connecting across different networks".
window.PRESENCE_CONFIG = {
  // Metered TURN credentials URL, e.g.
  // 'https://YOURAPP.metered.live/api/v1/turn/credentials?apiKey=YOUR_API_KEY'
  // Leave empty to use direct connections only (works when everyone is on the same Wi-Fi).
  turnCredentialsUrl: '',
};
